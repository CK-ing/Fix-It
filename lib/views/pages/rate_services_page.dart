import 'package:flutter/material.dart';

class RateServicesPage extends StatelessWidget {
  const RateServicesPage({super.key, required String bookingId, required String serviceId});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('RateServicesPage'),);
  }
}